{{ form_start(form) }}
  {{ form_rest(form) }}
  <button class="btn btn-primary">{{ button|default('Create') }}</button>
  <a href="{{ path('admin.feature.index') }}" class="btn btn-secondary">Cancel</a>
{{ form_end(form) }}
